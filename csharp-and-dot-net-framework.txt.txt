C# is a language, and .NET is an application framework (as it own name shows..). The .NET Framework libraries can run on the CLR and that is why any language which can run on the CLR can also use the .NET libraries.